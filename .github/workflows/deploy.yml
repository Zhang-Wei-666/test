name: Deploy to 

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v2
      - run: yarn install --frozen-lockfile
      - run: yarn build
      - run: |
         cd dist
         git init
         git config user.email "994039348@qq.com"
         git config user.name "Zhang-Wei-666"
         git add .
         git commit -m "update"
         git remote add origin https://github.com/Zhang-Wei-666/test2.git
         git push origin HEAD:master --force
